# Copy to apps/web/.env.local for local dev. NEVER commit real secrets.
# Both are non-secret publishable values (RLS allows read-only anon access).
# In production the app falls back to the public Kalibra project if unset.
SUPABASE_URL=https://mvrbfiztubrbbepwgnnm.supabase.co
SUPABASE_ANON_KEY=sb_publishable_ij95o_qtM4oKQ5jg7HodTg_74ppFN5v
